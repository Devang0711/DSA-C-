#ifndef VECTORDEMO_H
#define VECTORDEMO_H

namespace VectorDemo {
    void interactiveMenu();          // push_back, pop_back, insert, erase, resize, etc.
    void compareVectorVsArray();     // memory calculator: array vs vector for same data
    void run();                      // top-level menu wrapping the above
}

#endif // VECTORDEMO_H
